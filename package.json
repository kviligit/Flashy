{
  "name": "flashy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local-first flashcard webapp implementing FSRS-6.",
  "scripts": {
    "build": "npm run stamp && tsc",
    "watch": "npm run stamp && tsc --watch",
    "test": "npm run clean && npm run stamp && tsc && node tests/architecture.mjs && node --test \"dist/**/*.test.js\"",
    "dev": "npm run stamp && tsc && python3 -m http.server 5173",
    "clean": "rm -rf dist",
    "e2e": "npm run clean && npm run stamp && tsc && node tests/e2e.mjs",
    "check": "npm test && npm run e2e",
    "arch": "node tests/architecture.mjs",
    "stamp": "node scripts/stamp-build.mjs"
  },
  "devDependencies": {
    "typescript": "6.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}
